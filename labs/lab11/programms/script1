while getopts "i:o:p:C:n" opt
do
case $opt in
i)inputfile="$OPTARG";;
o)outputfile="$OPTARG";;
p)shablon="$OPTARG";;
c)register="";;
n)number="";;
*)echo Illegal option $opt
esac
done

grep -n "$shablon" "$inputfile" > "$outputfile"